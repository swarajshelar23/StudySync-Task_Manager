# Simple StudySync Frontend Demo

This folder contains a minimal Node.js + Express server that serves a static HTML/CSS demo and a tiny in-memory tasks API.

Run:

```bash
cd frontend/simple
npm install
npm start
# open http://localhost:4000
```
